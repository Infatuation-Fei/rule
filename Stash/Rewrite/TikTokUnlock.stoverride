name: TikTok解锁
desc: 需要安装Tiktok21.1.0版本，其它版本自测，rewrite第一行JP为观看的地区，默认JP(日本)，可自行修改为其它地区

http:
  mitm:
    - "*.tiktokv.com"
    - "*.byteoversea.com"
    - "*.tik-tokapi.com"
  rewrite:
    - (?<=_region=)CN(?=&) JP 307
    - (?<=&mcc_mnc=)4 2 307
    - ^(https?:\/\/(tnc|dm)[\w-]+\.\w+\.com\/.+)(\?)(.+) $1$3 302
    - (?<=\d\/\?\w{7}_\w{4}=)1[6-9]..(?=.?.?&) 17 307
